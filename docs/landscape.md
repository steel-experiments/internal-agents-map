# Internal Agents — Full Catalog

<!-- This file is GENERATED by scripts/build.py. Do not edit by hand. -->
<!-- To add or change an entry, edit data/agents/*.yaml and re-run the build. -->

_26 agents cataloged. Sorted alphabetically by company._

## Block — Builderbot

> A multi-agent orchestration layer built on goose + MCP that coordinates agents across Block's entire codebase, invoked in Slack to take a ticket end-to-end to a reviewed PR.

| | |
| --- | --- |
| **Year** | 2026 |
| **Status** | internal |
| **Domains** | coding, code-review |
| **Autonomy** | drafts-reviewed |
| **Headline metric** | ~1,500 PRs merged per week (~15% of all production code changes at Block) |

**Architecture**

- **Sandbox:** Not detailed publicly
- **Harness:** Multi-agent orchestration built on goose (open-source agent framework) + MCP; multi-player, real-time, operating inside Slack threads
- **Model:** goose framework; model not specified
- **Tool Access:** MCP connects agents to internal tools and data; picks up Linear/Jira tickets, creates the branch, writes code, opens the PR, watches CI
- **Interfaces:** ['slack', 'linear', 'jira', 'github']
- **Knowledge:** Company-wide code context across hundreds of millions of lines and hundreds of services; Block also frames Builderbot as an 'agentic protector' around its software world model

**Primitives**

- **Orchestration layer** — Coordinates multiple agents over one codebase instead of running a single loop
- **Ticket-driven flow** — Linear/Jira ticket -> branch -> code -> PR -> CI watch, end to end

**Key metrics**

- 200,000+ operations per day
- ~1,500 pull requests merged per week (~15% of all production code changes at Block)
- "What used to take months now takes days"

**Lessons learned**

- Concentrate investment on orchestration, context, and the environment — let engineers focus on the problems worth solving
- Meet people in Slack: tag @builderbot with a short description and it works in the thread
- Multi-player real-time collaboration lets humans steer research, planning, and implementation rather than only reviewing after the fact

**Sources**

- [Block rolls out Builderbot, a new suite of AI-native tools that changes the way we ship](https://block.xyz/inside/block-rolls-out-builderbot-a-new-suite-of-ai-native-tools-that-changes-the-way-we-ship) _(blog)_
- [Protecting our systems with intelligence (Builderbot architecture)](https://engineering.block.xyz/blog/protecting-our-systems-with-intelligence) _(blog)_

---

## Brex — Internal Agent Platform

> Retool-based internal platform where employees build, test, and deploy agents for KYC, disputes, QA, collections, and operations.

| | |
| --- | --- |
| **Year** | 2026 |
| **Status** | internal |
| **Domains** | finance-ops, support, customer-success |
| **Autonomy** | human-in-loop |
| **Headline metric** | 50%+ of support cases resolved by AI as first touch; disputes cut from 3h to 3s |

**Architecture**

- **Sandbox:** Retool-hosted runtime (no bespoke execution env described)
- **Harness:** Retool-based builder with prompt management and multi-model testing/evaluation; built by a ~25-person systems-engineering team
- **Model:** Multi-model
- **Tool Access:** An MCP server exposes external product features to the internal platform; new product tools become internally available immediately; invoked via Slack /c1
- **Interfaces:** ['slack', 'internal-retool-ui']
- **Knowledge:** SOPs uploaded as a knowledge base (e.g. 100+ page dispute guides); customer account data
- **Credentials:** SSO via internal Retool proxies (no per-user accounts); ConductorOne access management; Okta auth; data classified by risk; ≤30-day retention, no training on inputs

**Primitives**

- **Prompt + eval studio** — Non-technical ops staff design prompts, test across models, deploy with QA oversight
- **MCP tool bridge** — Product features exposed to internal agents through one MCP server

**Key metrics**

- 50%+ of customer-support cases resolved by chatbot as first touch
- Dispute processing: 3 hours → 3 seconds
- QA coverage 100% (previously sample-based); 1 person replaces ~5 QA specialists
- KYC adverse-media accuracy 85% → 88%

**Lessons learned**

- Target 40% automation, not 100% — the last mile drives investments that often yield zero value
- Treat internal ops with external rigor — build internal platforms to product quality
- Don't skip the human in the middle — end-to-end automation fails on accuracy
- Map workflows to the discrete steps a human would take, then translate each to LLM instructions
- Architecture beats vendor — legal/data approvals should follow data-handling characteristics, not the tool name

**Sources**

- [Agent, Human, Ops: How Brex Is Changing Roles and Workflows](https://www.firstround.com/reviews/agent-human-ops-how-brex-is-changing-roles-and-workflows) _(case-study)_

---

## Browserbase — bb

> One generalized agent in Slack that writes PRs, investigates sessions, queries the warehouse, logs feature requests, and runs browser agents across engineering, ops, sales, and support.

| | |
| --- | --- |
| **Year** | 2026 |
| **Status** | internal |
| **Domains** | coding, code-review, support, customer-success, research |
| **Autonomy** | drafts-reviewed |
| **Headline metric** | Feature-request pipeline at 100% coverage with zero human effort |

**Architecture**

- **Sandbox:** Ephemeral Linux VM; pre-warmed snapshot rebuilt every 30 min; idles out after 30 min
- **Harness:** OpenCode core loop with 6 tools (read/write/edit/exec/safebash/skill)
- **Model:** Frontier models
- **Tool Access:** exec routes through a serverless integration proxy (Snowflake, HubSpot, Pylon, Grafana); the sandbox never sees real secrets
- **Interfaces:** ['slack', 'web-ui', 'webhook']
- **Knowledge:** Key repos cloned into /knowledge/; skills (markdown) loaded on demand
- **Credentials:** Credential brokering — the sandbox boots with references + rotating session tokens only; the proxy holds real creds; egress injection for a few hosts

**Primitives**

- **Skills (progressive disclosure)** — Markdown playbooks lazy-loaded per task so the general agent stays small
- **Integration proxy + RBAC/ABAC** — Scoped permissions per session make misbehavior structurally impossible
- **Credential brokering** — The sandbox runs arbitrary code yet never touches a secret

**Key metrics**

- Feature-request pipeline at 100% coverage, zero human effort
- 99% of first-response times < 24 hrs
- Session investigation: 30–60 min of log-diving → one Slack message

**Lessons learned**

- One agent with good abstractions beats a fleet of narrow bots
- Separate capabilities from the core loop — domain logic lives in skills + service packages
- Don't trust the model, remove its ability to do wrong: scope tools and services per invocation source
- Meet people where they are — Slack is the highest-leverage surface because that's where work already happens

**Sources**

- [How we build internal agents at Browserbase](https://browserbase.com/blog/internal-agents) _(blog)_

---

## Cloudflare — Internal AI engineering stack

> An internal platform of MCP servers, an access layer, and AI tooling (incl. an AI code reviewer) that makes agents useful inside Cloudflare.

| | |
| --- | --- |
| **Year** | 2026 |
| **Status** | internal |
| **Domains** | coding, code-review |
| **Autonomy** | drafts-reviewed |
| **Headline metric** | 3,683 internal users (60% of company); 47.95M AI requests / month |

**Architecture**

- **Sandbox:** Dynamic Workers for sandboxed code execution; Sandbox SDK to clone/build/test
- **Harness:** OpenCode + Windsurf clients; Agents SDK (McpAgent + Durable Objects) for stateful sessions
- **Model:** Workers AI (open-weight, on-platform) + frontier models (Opus, GPT), routed by task
- **Tool Access:** MCP Server Portal — one OAuth point aggregating 182+ tools from 13 servers; AI Gateway for routing, cost, BYOK, ZDR
- **Interfaces:** ['cli', 'ci', 'web']
- **Knowledge:** Backstage catalog (2,055 services) + AGENTS.md generated across ~3,900 repos
- **Credentials:** Zero API keys on client machines — a Worker injects keys server-side; Cloudflare Access (Zero Trust) auth
- **Context Mgmt:** Code Mode collapses upstream tool schemas into search + execute, holding token overhead constant at scale

**Primitives**

- **MCP Server Portal** — One OAuth aggregation point for all MCP tools
- **Code Mode** — Collapse N tool schemas into 2 calls to hold token overhead constant at scale
- **AGENTS.md** — Structured, generated repo context (runtime, nav, conventions, boundaries, deps)
- **AI Code Reviewer** — Multi-agent CI review: risk tiering, specialist agents, Codex-rule citations

**Key metrics**

- 3,683 internal users (60% of company, 93% of R&D)
- 47.95M AI requests / month; 241.37B tokens via AI Gateway
- Merge-request rate ~doubled to 10,952 / week
- 295 teams using agentic AI tools

**Lessons learned**

- Centralize through a proxy early — direct-to-gateway looks simpler but blocks per-user attribution, model cataloging, and policy later
- Without structured data, agents are working blind — they read code but can't see the system around it (Backstage / AGENTS.md)
- Tool schemas eat context (34 GitLab tools ≈ 7.5% of a 200K window) — collapse them at the portal
- Frontier + open-source hybrid: route a growing share of workloads to cheaper self-hosted models

**Sources**

- [The AI engineering stack we built internally](https://blog.cloudflare.com/internal-ai-engineering-stack/) _(blog)_

---

## Coinbase — Forge / Mux

> Forge turns a Slack/GitHub/Linear discussion into a Linear issue, fix, PR, and one-off build; Mux lets employees run many coding agents concurrently.

| | |
| --- | --- |
| **Year** | 2026 |
| **Status** | internal |
| **Domains** | coding, code-review |
| **Autonomy** | drafts-reviewed |
| **Headline metric** | Mux: 600+ users across Coinbase organizations (April 2026) |

**Architecture**

- **Sandbox:** Mux gives each concurrent agent its own git worktree, branch, and terminal
- **Harness:** Portfolio approach — Claude Code, OpenCode, Cursor, and Copilot rather than one harness; Forge is a custom harness invokable from Slack, GitHub, and Linear
- **Model:** Portfolio across multiple providers
- **Tool Access:** Linear treated as the structured product context / source of truth
- **Interfaces:** ['slack', 'github', 'linear']
- **Knowledge:** Linear as the durable structured-context layer for product work

**Primitives**

- **Forge** — Custom harness: Slack bug discussion → Linear issue → fix → PR → one-off build
- **Mux** — Concurrency layer — one human coordinates many isolated coding agents, each in its own worktree/branch/terminal

**Key metrics**

- Mux: 600+ users across Coinbase organizations (April 2026)

**Lessons learned**

- Support a portfolio of harnesses (Claude Code, OpenCode, Cursor, Copilot) rather than standardizing on one
- Keep the system of record (Linear) as the agent's structured context — conversation can be the input, Linear stays durable
- Simple per-agent worktree/branch/terminal isolation is a pragmatic alternative to full sandboxing for concurrent coding

**Sources**

- [Coding had a concurrency problem: how Mux helped solve it](https://www.coinbase.com/de/blog/coding-had-a-concurrency-problem-how-mux-helped-solve-it) _(blog)_
- [Coinbase × Linear (Forge workflow)](https://linear.app/customers/coinbase) _(case-study)_

---

## Domu — Clementino

> A general-purpose 'AI colleague' spanning sales, finance, client ops, engineering, and recruitment, later split into a reusable toolkit plus Slack and desktop surfaces.

| | |
| --- | --- |
| **Year** | 2026 |
| **Status** | internal |
| **Domains** | support, finance-ops, coding, recruitment, customer-success |
| **Autonomy** | human-in-loop |
| **Headline metric** | ~35 integrations organized into skills; customer-impacting actions behind Slack approval gates |

**Architecture**

- **Harness:** Claude/Anthropic SDK wrapper; a reusable tool/skill/memory layer separated from the Slack and desktop interfaces
- **Model:** Claude (Anthropic SDK)
- **Tool Access:** ~35 integrations organized into skills; specialist delegates per domain
- **Interfaces:** ['slack', 'desktop']
- **Knowledge:** Four memory layers: conversation context, persistent facts, knowledge RAG, and live system state
- **Credentials:** Customer-impacting actions gated behind team-visible Slack approvals
- **Context Mgmt:** Four-layer memory separation; prompt size dropped substantially after splitting capabilities from the Slack layer

**Primitives**

- **Four memory layers** — Transient conversation, persistent facts, knowledge RAG, and live system state — kept separate
- **Reusable toolkit split** — Capabilities separated from the interface layer so they power multiple surfaces

**Key metrics**

- ~35 integrations organized into skills

**Lessons learned**

- Separate reusable capabilities (tools/skills/memory) from the interface layer — prompt size drops while capability is preserved
- Model memory explicitly: conversation context, persistent facts, RAG, and live state have different lifecycles
- Gate customer-impacting actions behind human approvals rather than trusting the model

**Sources**

- [Why we split our internal agent in two](https://domu.ai/blog/why-we-split-our-internal-agent-in-two) _(blog)_

---

## DoorDash — AI Code Review Agent

> A specialized agent that automatically reviews 10,000+ PRs a week across 56 repositories, emphasizing grounded high-confidence findings over noisy comments.

| | |
| --- | --- |
| **Year** | 2026 |
| **Status** | internal |
| **Domains** | code-review |
| **Autonomy** | drafts-reviewed |
| **Headline metric** | 10,000+ PRs reviewed per week across 56 repositories; 60.2% action rate on high/critical findings |

**Architecture**

- **Harness:** Three architecture versions; emphasis on attention and grounded, high-confidence findings rather than commenting everywhere
- **Model:** Not specified
- **Tool Access:** Reviews Go, iOS, Android, web, infrastructure, and data code
- **Interfaces:** ['github']
- **Knowledge:** Grounded findings tied to evidence

**Primitives**

- **Grounded findings** — High-confidence, evidence-backed comments rather than blanket commentary

**Key metrics**

- 10,000+ PRs reviewed in a typical week across 56 repositories
- 60.2% action rate on settled high/critical findings (measured sample)

**Lessons learned**

- Optimize for attention — minimize noisy comments; comment only with grounded, high-confidence findings
- Measure whether engineers actually act on findings (action rate), not comment volume

**Sources**

- [How DoorDash built an AI code reviewer engineers actually listen to](https://careersatdoordash.com/blog/doordash-built-an-ai-code-reviewer-engineers-actually-listen-to/) _(blog)_

---

## DoorDash — Flux / Agentic AI Platform

> DoorDash's internal agentic AI platform — a unified cognitive layer over company data and operations, with an AI Marketplace of specialized agents and the Flux cloud-agent runtime for engineering tasks.

| | |
| --- | --- |
| **Year** | 2026 |
| **Status** | internal |
| **Domains** | code-review, coding, ci-triage, on-call, maintenance, data |
| **Autonomy** | drafts-reviewed |
| **Headline metric** | 130,000 engineering tasks automated in one month; 10,000+ PRs reviewed per week |

**Architecture**

- **Sandbox:** Firecracker microVMs; <5s p95 end-to-end setup (boot, clone repos, install tools, configure harness)
- **Harness:** Maturity model: deterministic workflows -> ReAct agents -> hierarchical deep agents -> experimental swarms
- **Model:** Multiple frontier models
- **Tool Access:** In-house MCP gateway ('Agent Gateway'); LangGraph orchestration; prospective A2A; tools declared per playbook with scoped, logged permissions
- **Interfaces:** ['slack', 'github', 'cron', 'cli', 'conversational-skill', 'cursor']
- **Knowledge:** AI Marketplace of specialized agents; DataExplorer for grounded analytics; DoorDash-specific context in playbooks
- **Credentials:** Scoped per playbook; brokered through the gateway, never on the laptop; provenance on every action
- **Context Mgmt:** Hybrid retrieval: BM25 + dense semantic + reciprocal-rank fusion -> RAG; schema-aware SQL with EXPLAIN validation

**Primitives**

- **Sandbox** — Isolated Firecracker microVM with repos, tools, secrets, runtime deps
- **MCP Gateway** — Governed, audited access to CI, observability, issue trackers, deploy, code search
- **Playbook** — YAML unit of agentic work: task, inputs, skills, tools, permissions, validation, outputs
- **DataExplorer** — Identifies schemas, generates grounded SQL, validates via EXPLAIN before execution
- **Maturity model** — Workflows -> agents -> deep-agent hierarchies -> swarms; governance hardens as control decentralizes

**Key metrics**

- 130,000 engineering tasks automated in one month
- 25,000+ automated code reviews per week
- 300+ playbooks; 10,000+ invocations per week

**Lessons learned**

- Start narrow to earn trust — began with automated code review before CI triage, on-call, maintenance, ticket-driven dev
- Make the work visible — public Slack threads drove adoption; private per-run channels did not build team habits
- Playbooks need enablement — workshops and hackathons turn repeated operational work into reusable playbooks
- Earn complexity by exhausting simpler primitives first — keep swarms at the research frontier until governance catches up
- Deterministic verification before probabilistic judgment — SQL linting and EXPLAIN before deeper validation; LLM-as-judge + DeepEval
- Log provenance so any answer traces back to source queries, documents, and inter-agent activity

**Sources**

- [Delegating Engineering Work To Cloud-Based Agents (Flux)](https://x.com/AIatDoorDash/status/2087285008906240193) _(post)_
- [Beyond single agents: DoorDash's collaborative AI ecosystem](https://careersatdoordash.com/blog/beyond-single-agents-doordash-building-collaborative-ai-ecosystem/) _(blog)_

---

## Dropbox — Nova

> An internal platform for coding agents: engineers launch parallel sessions and internal systems invoke agents inside automated SDLC workflows.

| | |
| --- | --- |
| **Year** | 2026 |
| **Status** | internal |
| **Domains** | coding, ci-triage, on-call, maintenance |
| **Autonomy** | human-in-loop |
| **Headline metric** | Dozens of agents in parallel from one runbook; migrations across thousands of entries |

**Architecture**

- **Sandbox:** Isolated env with a codebase snapshot at a specific commit; full Dropbox monorepo via Bazel; hermetic remote execution + caching
- **Harness:** Validation loop (propose → validate → feed back) with continue_on_validation_failure and max_iterations (~5); branch management kept outside the agent
- **Model:** Platform-agnostic — multiple coding agents behind one interface; swap models without rebuilding infra; prompt-eval tooling
- **Tool Access:** Skills/plugins to gather evidence, read logs, inspect failures; MCP integrations; Bazel-aware selectivity tools
- **Interfaces:** ['web-ui', 'cli', 'api', 'slack']
- **Knowledge:** Localized AGENTS.md per service; Dash (Dropbox context engineering); passing + failing test logs
- **Credentials:** Operates within Dropbox's existing infra and validation paths; same auth/authz as engineers
- **Context Mgmt:** Session history (notes/logs) carried across retry attempts

**Primitives**

- **Validation loop** — Bounded iteration with feedback on failure; deterministic systems control test execution
- **Dash** — Dropbox context-engineering system feeding agents across the SDLC

**Key metrics**

- Flaky-test remediation (Deflaker): 100+ validation runs
- Migrations applied across thousands of entries in parallel
- Dozens of agents launchable from one runbook

**Lessons learned**

- Platform value exceeds code generation — validation, guardrails, and context matter as much
- Context, validation, and guardrails reinforce each other to make background work trustworthy
- Not every step belongs in the agent loop — deterministic systems should control test execution and timing
- Integrate with existing engineering infrastructure rather than building separate AI-specific workflows

**Sources**

- [Introducing Nova, our internal platform for coding agents](https://dropbox.tech/machine-learning/introducing-nova-our-internal-platform-for-coding-agents) _(blog)_

---

## Flex — AI Investigation Agent

> A Slack agent for HSA/FSA payment operations that traces a payment end-to-end and, when it finds a software bug, prepares a PR with a proposed fix.

| | |
| --- | --- |
| **Year** | 2026 |
| **Status** | internal |
| **Domains** | finance-ops, on-call, coding |
| **Autonomy** | drafts-reviewed |
| **Headline metric** | Used by the team every day for payment-ops investigation |

**Architecture**

- **Sandbox:** Not specified publicly
- **Harness:** Investigation-to-fix loop; underlying runtime not documented
- **Model:** Not specified
- **Tool Access:** Traces a payment end-to-end across payment systems; can open a PR with a proposed fix when a bug is found
- **Interfaces:** ['slack']

**Primitives**

- **Investigation-to-fix loop** — Payment trace → root-cause hypothesis → proposed code fix as a PR

**Lessons learned**

- Start where correctness is observable — payment investigation produces artifacts (a trace, a hypothesis, a diff) that can be checked
- An ops agent that can prepare a fix (not just a report) closes the loop from investigation to code

**Sources**

- [The Flex AI Investigation Agent for HSA/FSA payments](https://www.withflex.com/blog/the-flex-ai-investigation-agent-for-hsa-fsa-payments) _(blog)_

---

## Harvey — Spectre

> Harvey's internal collaborative cloud agent platform — reacts to incidents, bug reports, and Slack messages and produces reviewable diffs, branches, and PRs.

| | |
| --- | --- |
| **Year** | 2026 |
| **Status** | internal |
| **Domains** | coding, code-review, on-call, security |
| **Autonomy** | drafts-reviewed |
| **Headline metric** | Extends to an always-on agentic security operations center |

**Architecture**

- **Sandbox:** Isolated ephemeral execution environments; durable runs
- **Harness:** Collaborative cloud agent platform with explicit boundaries around GitHub, Datadog, Linear, etc.
- **Model:** Not specified
- **Tool Access:** Explicit tool boundaries; reacts to incidents, bug reports, customer feedback, and Slack messages
- **Interfaces:** ['slack', 'web', 'automations']
- **Credentials:** Explicit tool boundaries; outputs are reviewable
- **Context Mgmt:** Durable runs over disposable execution environments

**Primitives**

- **Durable runs / ephemeral execution** — Long-lived run state over throwaway compute
- **Reviewable artifacts** — Outputs are summaries, diffs, branches, PRs — not silent actions

**Lessons learned**

- Make outputs reviewable (diffs, branches, PRs) rather than letting the agent act silently
- Implementation speed shifts the bottleneck toward review, prioritization, and coordination
- The same agent substrate extends beyond engineering (e.g. an agentic SOC)

**Sources**

- [Building Spectre — internal collaborative cloud agent platform](https://www.harvey.ai/blog/building-spectre-internal-collaborative-cloud-agent-platform) _(blog)_
- [Building an agentic security operations center](https://www.harvey.ai/blog/building-an-agentic-security-operations-center) _(blog)_

---

## Linear — Linear Agent

> A native agent that synthesizes workspace context, triages, creates follow-up work, runs coding sessions, and executes scheduled/event-driven 'Loops' — used by Linear's own CX, Product, and Engineering teams.

| | |
| --- | --- |
| **Year** | 2026 |
| **Status** | internal |
| **Domains** | support, customer-success, coding |
| **Autonomy** | drafts-reviewed |
| **Headline metric** | Customer request -> scoped issue -> working PR, end to end |

**Architecture**

- **Sandbox:** Not detailed publicly
- **Harness:** Separate planning agent (triage/issue creation) and coding agent (code generation); Code Intelligence for codebase knowledge; scheduled/event-driven 'Loops'
- **Model:** GPT-5 + Gemini 2.5 Pro in Triage Intelligence; Claude Code or Codex for coding sessions
- **Tool Access:** Triage Intelligence (auto-route, dedup, label); GitHub; testing Code Intelligence + custom MCP servers
- **Interfaces:** ['slack', 'intercom', 'linear-app', 'github']
- **Knowledge:** Semantic/vector search evolved into agentic context acquisition across the workspace; Datadog/Sentry customer context
- **Credentials:** The Agent SDK gives agents explicit identities, scoped OAuth tokens, assignable/mentionable handles, and visible human delegation — issues stay assigned to a human; 'an agent cannot be held accountable'

**Primitives**

- **Triage Intelligence** — Auto-routes issues, flags duplicates, suggests labels
- **Agent SDK identities** — Agents get identities, scoped team access, and visible delegation alongside humans
- **Loops** — Scheduled or event-driven agent runs that execute recurring work

**Lessons learned**

- Keep the agent close to the source of work (Intercom, Slack, Linear) — the best workflows live where work already happens
- Gradual autonomy — start by asking for suggestions, observe, add guidance, only automate once proven reliable
- Break work into small steps to keep coding agents focused and successful
- AI mistakes are useful — they surface failure modes to engineers
- Close the loop — auto-notify the customer when their request ships

**Sources**

- [How we built Linear Agent](https://linear.app/now/how-we-built-linear-agent) _(blog)_
- [How we use Linear Agent at Linear](https://linear.app/now/how-we-use-linear-agent-at-linear) _(blog)_
- [Our approach to building the Agent Interaction SDK](https://linear.app/now/our-approach-to-building-the-agent-interaction-sdk) _(blog)_

---

## monday.com — Sphera / Atlas / Morphex

> An internal agent system on Amazon Bedrock where agents have identities, managers, scopes, and performance scores; Atlas ships features, Morphex ships PRs autonomously.

| | |
| --- | --- |
| **Year** | 2026 |
| **Status** | internal |
| **Domains** | coding, code-review |
| **Autonomy** | autonomous |
| **Headline metric** | Morphex: 19 of 20 PRs merge without human review |

**Architecture**

- **Sandbox:** Amazon EKS, one pod per active session; a remote sandbox tests each PR before review; EFS workspace mount
- **Harness:** Claude Agent SDK behind a thin monday-agent-sdk wrapper (provider neutrality, cold-start optimization, custom harness opinions)
- **Model:** Amazon Bedrock; Application Inference Profiles for routing; cross-region failover; PrivateLink (traffic stays in VPC)
- **Tool Access:** Triggers via Slack @mention, monday item assignment, or GitHub PR review → SNS → per-team SQS → consumers; monday MCP servers underpin the Guardrails
- **Interfaces:** ['slack', 'monday', 'github']
- **Knowledge:** File-based memory: MEMORY.md (cross-session) + diary/YYYY-MM-DD.md; sessions/repos/secrets on EFS; durable records on S3
- **Credentials:** Per-session secrets in AWS Secrets Manager; same RBAC as humans; real Slack/GitHub/monday accounts
- **Context Mgmt:** Live state in ElastiCache (sub-ms); monday boards (Builders CoWORK) as shared state for tasks, status, handoffs

**Primitives**

- **Agent profiles** — Stable identity + assigned human manager + scope + performance score
- **PR Guardrails** — Automated review against monday standards (metrics, feature flags, security)
- **Memory as files** — MEMORY.md + daily diary instead of vector retrieval
- **Builders CoWORK** — monday boards as the shared-state layer for human/agent collaboration

**Key metrics**

- Morphex: 19 of 20 PRs merge automatically without human review
- 90% of engineers use AI coding tools monthly (up from ~50% YoY)
- Per-engineer PR throughput +50%
- Guardrails catches ~25% of agent PRs before human review; low single-digit revert rate

**Lessons learned**

- Evals from day one — should have been day one, not month nine
- Skip the vector store — file-based memory (MEMORY.md) was the right answer
- Remote-sandbox every PR before human review, with production-traffic replay
- The existing auth/identity/deploy pipeline applies to agents — reuse it
- AI engineering is building the feedback loops that let imperfect agents be trusted safely

**Sources**

- [AI Teammates: how monday.com runs production AI agents on Amazon Bedrock](https://aws.amazon.com/blogs/machine-learning/ai-teammates-how-monday-com-runs-production-ai-agents-on-amazon-bedrock/) _(case-study)_

---

## Ramp — Inspect

> A background coding agent that closes the loop on verifying its own work — runs tests, reviews telemetry, queries feature flags, visually verifies the frontend — now also monitoring production and proposing fixes.

| | |
| --- | --- |
| **Year** | 2026 |
| **Status** | internal |
| **Domains** | coding, code-review, on-call |
| **Autonomy** | drafts-reviewed |
| **Headline metric** | 60%+ of Ramp's merged PRs authored by Inspect (April 2026) |

**Architecture**

- **Sandbox:** Modal sandboxes; per-repo images rebuilt every 30 min from snapshots; warm-on-keystroke; a pool of warm sandboxes
- **Harness:** OpenCode (server-first) as the agent runtime; a plugin blocks writes until sync completes; expanded into production monitoring and self-maintenance
- **Model:** All frontier models, MCPs, custom tools, skills
- **Tool Access:** Wired into Sentry, Datadog, LaunchDarkly, Braintrust, GitHub, Slack, Buildkite; monitors production, triages issues, proposes fixes
- **Interfaces:** ['slack', 'web', 'chrome-extension', 'pull-request']
- **Knowledge:** Skills that encode how Ramp ships; repo images with the full dev env (Vite, Postgres, Temporal)
- **Credentials:** GitHub auth per user; the sandbox pushes the branch, an API opens the PR with the user's token (no self-approval); production merges retain human review

**Primitives**

- **Modal sandbox + snapshots** — Pre-warmed full dev envs; fast cold start; effectively free to run
- **OpenCode runtime** — Server-first agent with a typed SDK + plugin system; code is its own source of truth
- **Multiplayer sessions** — Any number of people in one session; each change attributed to its author

**Key metrics**

- 60%+ of merged PRs authored by Inspect (April 2026)
- ~30% of merged PRs (frontend + backend) at launch
- Session speed limited only by model-provider time-to-first-token

**Lessons learned**

- Own the tooling — it only has to work on your code, which lets you build something more powerful than off-the-shelf
- Work in public spaces to create virality loops; let the product do the talking, don't mandate
- Background agents that are fast are strictly better than local: same intelligence, more power, unlimited concurrency
- Move as much as possible into the image-build step so users never wait on setup

**Sources**

- [Why We Built Our Own Background Agent (Inspect)](https://engineering.ramp.com/post/why-we-built-our-background-agent) _(blog)_
- [Ramp x Linear (60%+ of merged PRs via Inspect)](https://linear.app/customers/ramp) _(case-study)_

---

## Replit — Manager agent (agent-of-agents)

> An internal agent-of-agents stack where every employee gets a manager agent that spawns multiple agents for verifiable work and escalates judgment to humans.

| | |
| --- | --- |
| **Year** | 2026 |
| **Status** | internal |
| **Domains** | coding, code-review, support, research, data |
| **Autonomy** | drafts-reviewed |
| **Headline metric** | ~3x engineering code output over six months without degradation in review/revert/incident metrics |

**Architecture**

- **Sandbox:** Not specified publicly
- **Harness:** Fleet/loop orchestration: a manager agent launches parallel agents for verifiable work and escalates judgment
- **Model:** Not specified
- **Tool Access:** Investigates incidents, reviews PRs, answers questions, analyzes company data, triages support, researches sales accounts, improves Replit Agent itself
- **Interfaces:** ['slack']
- **Context Mgmt:** Manager agent coordinates parallel sub-agents and routes results

**Primitives**

- **Manager agent** — One human gives an objective; the manager spawns parallel agents for verifiable work and escalates judgment

**Key metrics**

- ~3x engineering code output over six months
- No corresponding deterioration in review/reversion/incident metrics

**Lessons learned**

- Give every employee a manager agent that spawns sub-agents — verifiable work parallelizes, judgment escalates to humans
- Track outcome metrics (reverts, incidents), not activity — output can scale without quality regressions

**Sources**

- [The Self-Driving Company](https://ld.replit.com/blog/self-driving-company) _(blog)_

---

## Salesforce — Slackbot

> Salesforce was 'customer zero' for the rebuilt Slackbot — an employee agent that finds company context, drafts work, and connects Slack context with Salesforce data; now also an external product.

| | |
| --- | --- |
| **Year** | 2026 |
| **Status** | commercialized |
| **Domains** | support, customer-success, ops |
| **Autonomy** | drafts-reviewed |
| **Headline metric** | Permission-aware by construction — sees only what the employee can see |

**Architecture**

- **Harness:** An employee agent intended as a front door to other agents
- **Model:** Not specified
- **Tool Access:** Finds company context, drafts work, manages meetings, connects Slack context with Salesforce data
- **Interfaces:** ['slack']
- **Knowledge:** Slack context joined with Salesforce CRM data
- **Credentials:** Permission-aware by construction — sees what the employee can see, respects roles and access controls

**Primitives**

- **Permission-aware context** — Agent visibility is bounded by the invoking employee's permissions

**Lessons learned**

- Dogfood internally first ('customer zero') before shipping externally
- Make permission-awareness a construction property, not a prompt instruction — the agent sees only what the employee can see

**Sources**

- [Salesforce announces general availability of Slackbot](https://www.salesforce.com/ap/news/press-releases/2026/01/14/salesforce-announces-the-general-availability-of-slackbot-your-personal-agent-for-work-sg/) _(blog)_

---

## Sentry — Junior

> An open-source Slack agent built at Sentry that acts like an intern — takes tasks, retrieves context across many company systems, and is steered and reviewed by humans. Its CEO argues one general-purpose agent beat several vendor-specific bots.

| | |
| --- | --- |
| **Year** | 2025 |
| **Status** | open-sourced |
| **Domains** | coding, code-review, support, on-call |
| **Autonomy** | human-in-loop |
| **Headline metric** | Open-source (Apache-2.0) Slack agent (~100k lines of TS) used internally at Sentry |

**Architecture**

- **Sandbox:** Vercel serverless functions; Vercel agent-browser sandbox with a MITM proxy for traffic interception; ephemeral containers
- **Harness:** Custom harness on Pi's SDK; a task broker over Vercel Queues with an inbox -> worker-claim -> interrupt/resume pattern to survive serverless timeouts; 'skills-as-runbooks'
- **Model:** Claude Sonnet (faster); swappable (Opus as a more expensive option)
- **Tool Access:** Progressive discovery via MCP — by default Junior connects to no provider until the agent requests a tool lookup; plugins connect Sentry, GitHub, Linear, Notion
- **Interfaces:** ['slack', 'web-dashboard', 'github']
- **Knowledge:** Conversation transcripts persisted in Redis; repo search to trace code paths; skill docs (TELEMETRY.md, SOUL.md)
- **Credentials:** MITM-proxy injection — the model never sees the token because it's never in the sandbox; plugins declare OAuth flows and credential domains; GitHub distinguishes read vs. write
- **Context Mgmt:** Incremental transcript updates in Redis; resource subscriptions to GitHub PR events for follow-ups

**Primitives**

- **Progressive tool discovery** — searchMcpTools loads tools on demand instead of dumping every schema into the prompt
- **MITM credential proxy** — Credentials injected host-side; the sandbox/model never touches a secret
- **Interrupt/resume broker** — Survives serverless timeouts via a queue + claim model

**Key metrics**

- ~100,000 lines of TypeScript (excluding tests)
- 4 months from start to writeup

**Lessons learned**

- One general-purpose agent connected to many company systems beats several vendor-specific bots
- Skills-as-runbooks encode operational knowledge the agent can follow
- Stateless compute fights you — serverless functions time out and disappear; model the agent around interrupt/resume
- Unit tests are the wrong yardstick for agents — invest in evals and integration tests instead
- Writes need per-user authorization, not blanket trust

**Sources**

- [Building an Intern (Junior at Sentry)](https://cra.mr/building-an-intern/) _(blog)_
- [getsentry/junior (open-source, Apache-2.0)](https://github.com/getsentry/junior) _(docs)_
- [Sentry Labs](https://labs.sentry.dev/) _(docs)_

---

## Shopify — Aquifer / River

> The Aquifer agent platform (session/harness/sandbox split) powers River, a Slack-native coding agent, plus research, migration, and app-security agents.

| | |
| --- | --- |
| **Year** | 2026 |
| **Status** | internal |
| **Domains** | coding, code-review, research, security |
| **Autonomy** | drafts-reviewed |
| **Headline metric** | 1 in 8 merged PRs company-wide coauthored by River |

**Architecture**

- **Sandbox:** An execution environment (filesystem, shell, repo, build/test), separated from the harness — 'decouple brain from hands'
- **Harness:** Session (durable; Postgres append-only event log) + Harness (cheap agent loop) + Cell (ephemeral Go runtime); cells die, sessions persist
- **Model:** Swappable — the runtime is commoditizing; the value is in the harness + integrations
- **Tool Access:** Repo + tests + data warehouse + production traces + PR creation; a gateway credentials proxy
- **Interfaces:** ['slack', 'github']
- **Knowledge:** Monorepo 'World' (code + skills + conventions + intent docs + runbooks + AGENTS.md); Nix reproducible envs; Slack-transcript corpus mining
- **Credentials:** Gateway credentials proxy; per-profile sandbox policies; Shopify SSO
- **Context Mgmt:** Skills loaded on-demand as files, updatable per session; session survival across cell/sandbox/machine death

**Primitives**

- **Session/Harness/Sandbox split** — Durable identity, disposable loop, isolated execution — swap any layer independently
- **River (public-by-default)** — Slack-native coding agent in public channels only — visibility drives adoption
- **Skills as files** — Written-down knowledge mined from successful patterns and public transcripts

**Key metrics**

- River: 59,918 sessions / 30 days across 5,170 Slack channels
- 3,536 River-coauthored PRs merged; 1 in 8 merged PRs company-wide
- Median session 19 min; median 50 tool calls/session

**Lessons learned**

- Agent-friendly is human-friendly — monorepo, reproducible envs, written skills, and fast CI help both
- Local agents have a ceiling — private windows mean only the person at the keyboard learns anything
- Session survival is critical — cells die, sandboxes die, machines die; the conversation doesn't
- Treat agents as profiles, not platforms — a new agent is a new bundle on the same substrate

**Sources**

- [Under the River](https://shopify.engineering/under-the-river) _(blog)_

---

## Sierra — Pinecone

> One company-wide agent that collapsed separate support, analytics, engineering, and sales agents into a single runtime with an MCP Gateway to 37 systems.

| | |
| --- | --- |
| **Year** | 2026 |
| **Status** | internal |
| **Domains** | coding, code-review, support, research, data |
| **Autonomy** | drafts-reviewed |
| **Headline metric** | 75,000+ sessions for 600+ people; opened 70% of company PRs |

**Architecture**

- **Sandbox:** Agency layer reconciles recoverable Kubernetes runners; conversation/events/checkpoints stay durable separately
- **Harness:** App server + Agency + runners; intent-based model/environment routing (Claude Code + Codex)
- **Model:** Claude Code + Codex; routes by intent (planning, coding, prose)
- **Tool Access:** MCP Gateway connected to 37 systems; a network proxy decides whether privileged requests proceed and injects credentials after approval
- **Interfaces:** ['slack', 'web', 'linear', 'mobile']
- **Knowledge:** Durable sessions over disposable environments; Redis Streams
- **Credentials:** Gateway enforces the invoking employee's access at tool-call time and isolates customer data; the harness never holds the real secret
- **Context Mgmt:** Durable session state separated from ephemeral compute

**Primitives**

- **MCP Gateway (37 systems)** — One gateway spanning 37 systems, enforcing employee permissions at call time
- **Agency sandbox layer** — Recoverable K8s runners with a network proxy for credential injection
- **Intent-based routing** — Routes model and environment by request intent, independent of the tool layer

**Key metrics**

- 75,000+ sessions for 600+ people (July 2026)
- Opened 70% of company PRs
- Hundreds of automations

**Lessons learned**

- Collapse departmental bots into one agent — cross-functional jobs don't respect org-chart boundaries
- Own the routing/context/workflow layer; let models be interchangeable (different models win at planning, coding, prose)
- Enforce permissions at the tool-call layer via a gateway, not via prompt instructions
- Session counts and tool calls are usage, not value — track business outcomes instead

**Sources**

- [Pinecone: harnessing the wisdom of the workforce](https://sierra.ai/jp/blog/pinecone-harnessing-the-wisdom-of-the-workforce) _(blog)_
- [Building Sierra's MCP Gateway](https://sierra.ai/blog/building-sierras-mcp-gateway-an-engineering-iceberg) _(blog)_
- [Agency: secure, scalable sandboxes for agents](https://sierra.ai/es/blog/agency-secure-scalable-sandboxes-for-agents) _(blog)_

---

## Slack — Multi-agent context system

> A coordinator/dispatcher multi-agent design with structured context channels for long-running investigations spanning hundreds of steps.

| | |
| --- | --- |
| **Year** | 2026 |
| **Status** | internal |
| **Domains** | research |
| **Autonomy** | human-in-loop |
| **Headline metric** | Megabyte-scale investigations without overflowing the context window |

**Architecture**

- **Sandbox:** n/a — this is a context-management pattern, not a full execution platform
- **Harness:** Coordinator/dispatcher: a central coordinator dispatches to expert agents and to critic agents
- **Model:** Not specified
- **Tool Access:** Expert agents produce reports; critic agents evaluate them using evidence-inspection tools
- **Context Mgmt:** Three channels — Director's Journal (working memory), Critic's Review (credibility-weighted findings), Critic's Timeline (deduped chronological synthesis)

**Primitives**

- **Director's Journal** — Structured working memory: findings, decisions, questions, hypotheses
- **Critic's Review** — A truth filter with credibility scores over submitted findings
- **Critic's Timeline** — A chronological, deduped, conflict-resolved synthesis retained across steps

**Key metrics**

- Handles multi-agent runs spanning hundreds of requests and megabytes of output

**Lessons learned**

- Don't pass all information at every step — build structured summaries agents can reliably build on
- Separate expert agents (produce) from critic agents (evaluate); corroborated findings are prioritized
- Context management becomes its own subsystem once runs get long

**Sources**

- [How Slack manages context in long-running multi-agent systems](https://www.infoq.com/news/2026/04/slack-agent-context-management/) _(blog)_

---

## Spotify — Honk / Xirp

> Honk is Spotify's background coding agent (high confidence); Xirp is the workspace/context/session layer around it (medium confidence). Honk runs on a Claude-Agent-SDK harness in Kubernetes with deterministic CI verifiers and an LLM judge that can veto a run.

| | |
| --- | --- |
| **Year** | 2025 |
| **Status** | commercialized |
| **Domains** | coding, migrations, code-review |
| **Autonomy** | drafts-reviewed |
| **Headline metric** | 1,500+ merged AI-generated PRs; ~50% of Spotify PRs automated |

**Architecture**

- **Sandbox:** Honk runs in a constrained Kubernetes container (it does not inherit arbitrary engineer credentials); jobs execute inside Fleet Management / Fleetshift
- **Harness:** Honk: Claude Agent SDK + Spotify's own harness + Kubernetes pods; Honk v2 adds shared sessions, projects, and Chirp orchestration
- **Model:** Claude via the Agent SDK (Claude-centric); the surrounding platform is multi-model
- **Tool Access:** Limited, deliberate tool surface; deterministic CI verifiers run before a PR proceeds, then an independent LLM judge that can veto a run; MCP and CLI for org context
- **Interfaces:** ['slack', 'github', 'cli']
- **Knowledge:** Backstage/catalog ownership and developer-standardization primitives built over years, exposed via MCP/CLI
- **Credentials:** Runs in a constrained container rather than inheriting engineer credentials
- **Context Mgmt:** Fleet Management coordinates repo targeting, builds/tests, and PR workflow at scale; Xirp pairs sessions with Portal org context

**Primitives**

- **Deterministic-then-judge verification** — Build/test verifiers gate the PR; an LLM judge can veto the run afterward
- **Fleet management** — Target thousands of repos, run builds/tests, open and merge PRs at fleet scale
- **Xirp (workspace/context layer)** — The session/context surface around the agent rather than an autonomous worker itself

**Key metrics**

- 1,500+ merged AI-generated PRs (Honk)
- 60-90% time savings on migrations vs manual
- ~50% of Spotify PRs automated
- 36,000+ Xirp sessions; thousands of engineers

**Lessons learned**

- Run deterministic verification before probabilistic judgment — CI verifiers first, LLM judge second
- Constrain the agent's environment and tools rather than handing it engineer credentials
- Invest in org context (Backstage/catalog) before agentic dev accelerates
- Autonomy without structure fragments into per-engineer configs — shared org context is the multiplier

**Sources**

- [Spotify's Journey with Our Background Coding Agent (Honk)](https://engineering.atspotify.com/2025/11/spotifys-journey-with-our-background-coding-agent) _(blog)_
- [Code with Claude: coding is no longer the constraint (Honk v2)](https://engineering.atspotify.com/2026/6/code-with-claude-coding-is-no-longer-the-constraint) _(blog)_
- [Xirp - Powered by Spotify Portal](https://xirp.spotify.com/) _(docs)_

---

## Stripe — Minions

> Homegrown one-shot coding agents that read work context, locate the right repo/workspace, implement a change end-to-end, and produce a PR for human review.

| | |
| --- | --- |
| **Year** | 2026 |
| **Status** | internal |
| **Domains** | coding, code-review |
| **Autonomy** | drafts-reviewed |
| **Headline metric** | 1,000+ production PRs per week, reviewed and approved by humans |

**Architecture**

- **Sandbox:** Not specified publicly
- **Harness:** One-shot agents: read context -> search the codebase -> determine the relevant repo/workspace -> implement -> PR
- **Model:** Not specified
- **Tool Access:** Searches Stripe's codebase to determine the relevant repository/workspace
- **Interfaces:** ['slack', 'github']
- **Knowledge:** Stripe's codebase as the context source
- **Credentials:** Production PRs left for humans to review and approve

**Primitives**

- **One-shot end-to-end agent** — From work context to merge-ready PR in one shot, with human approval

**Key metrics**

- 1,000+ production PRs per week

**Lessons learned**

- One-shot end-to-end coding agents work at scale when humans retain review/approval
- Let the agent locate the right repo/workspace itself rather than pre-scoping it

**Sources**

- [Minions: Stripe's one-shot end-to-end coding agents](https://stripe.dev/blog/minions-stripes-one-shot-end-to-end-coding-agents) _(blog)_
- [Minions - Part Two](https://stripe.dev/blog/minions-stripes-one-shot-end-to-end-coding-agents-part-2) _(blog)_

---

## Uber — Internal coding agent (unnamed)

> Uber's internal coding agent, reported by its CTO as producing roughly 1,800 complete code changes per week.

| | |
| --- | --- |
| **Year** | 2026 |
| **Status** | internal |
| **Domains** | coding |
| **Autonomy** | drafts-reviewed |
| **Headline metric** | ~1,800 complete code changes per week (~8% of changes) |

**Architecture**

- **Sandbox:** Not specified publicly
- **Harness:** Not specified publicly

**Key metrics**

- ~1,800 complete code changes per week (~8% of changes at the time)
- 95% of engineers use AI tools monthly

**Sources**

- [Uber's CTO on AI coding agents (Business Insider)](https://www.businessinsider.com/uber-cto-ai-coding-agentic-software-engineers-2026-3) _(blog)_

---

## WorkOS — Project Horizon

> An internal autonomous 'code factory' where a continuously running swarm of agents handles the implementation loop while engineers focus on requirements and acceptance testing. Deliberately modular so the harness can evolve.

| | |
| --- | --- |
| **Year** | 2026 |
| **Status** | internal |
| **Domains** | coding, code-review, security |
| **Autonomy** | drafts-reviewed |
| **Headline metric** | A self-improving platform — every run ships work and feeds fixes back in |

**Architecture**

- **Sandbox:** Cloudflare Containers + Sandbox SDK; disposable, tightly scoped sandboxes with explicit lifecycle APIs and egress controls; full monorepo stack in Docker dev containers
- **Harness:** Modular by design — the core article runs OpenCode in the sandbox; the Applied AI Showcase runs Claude Remote Routines. The harness is swappable as agent tech changes; separate PM, implementation, and prospective verification/security roles
- **Model:** Swappable — the harness is the constant, not the model
- **Tool Access:** A custom MCP server stitches internal data sources (Datadog, Sentry, Slack, WorkOS Pipes); all outbound traffic proxied through Workers with allowlists, limits, logging, and token injection
- **Interfaces:** ['linear', 'github', 'slack', 'web']
- **Knowledge:** AGENTS.md and CLAUDE.md capture scripts, docs, conventions; MCP codifies the patterns engineers already follow; Notion + Figma for specs/mockups
- **Credentials:** WorkOS Pipes (no OAuth/token-refresh to maintain); scoped short-lived GitHub tokens per user; engineers use their own identity in the MCP; least-privilege + egress controls
- **Context Mgmt:** The orchestrator pauses/resumes sandboxes and tracks state + artifacts across a run

**Primitives**

- **Sandbox = execution primitive vs orchestrator = control plane** — Separate what runs code from what manages the lifecycle
- **Purpose-built agent infra** — A runtime controlled end-to-end, with lifecycle APIs and egress controls for the threat model
- **Self-improvement loop** — Each run ships work and produces the next set of fixes, surfacing where the platform is brittle
- **MCP as an iterative product** — Tuning tools is ongoing, not a one-time integration

**Lessons learned**

- You need purpose-built agent infrastructure — a runtime you control end-to-end with lifecycle APIs and egress controls
- Separate concerns: sandboxes are an execution primitive; the orchestrator is the control plane
- Build modularly so the harness can evolve — OpenCode today, Claude Remote Routines tomorrow, without rebuilding the platform
- Make autonomy a platform — the system gets faster and more reliable through use as fixes feed back in
- MCP tuning is an iterative product, not a one-time integration — codify the patterns engineers already follow

**Sources**

- [Project Horizon - an autonomous code factory at WorkOS](https://workos.com/blog/project-horizon) _(blog)_
- [Applied AI Showcase (Horizon with Claude Remote Routines)](https://workos.com/blog/applied-ai-showcase) _(blog)_
- [An autonomous UI-quality program](https://workos.com/blog/autonomous-ui-quality-program) _(blog)_

---

## Y Combinator — Internal agent infrastructure

> Internal agent infrastructure and own harnesses built from the ground up, framed as making AI the operating system the whole organization runs on.

| | |
| --- | --- |
| **Year** | 2026 |
| **Status** | internal |
| **Domains** | coding, ops |
| **Autonomy** | human-in-loop |
| **Headline metric** | Internal agent infra built end-to-end in-house (own harnesses) |

**Architecture**

- **Sandbox:** Not detailed publicly (primary source is a podcast)
- **Harness:** Own harnesses built from the ground up for internal AI use

**Lessons learned**

- Don't add AI as a feature — make it the operating system the whole organization runs on
- Build the harness and surrounding infra in-house rather than bolting onto a hosted agent

**Sources**

- [Inside YC's AI Playbook (Lightcone podcast, with Pete Koomen)](https://www.ycombinator.com/library/Qh-inside-yc-s-ai-playbook) _(podcast)_

---

## Zup — CodeGen

> A research-documented internal coding agent where constrained editing tools and layered safety controls mattered more than prompt tweaks.

| | |
| --- | --- |
| **Year** | 2026 |
| **Status** | internal |
| **Domains** | coding |
| **Autonomy** | human-in-loop |
| **Headline metric** | Targeted tool design and layered safety > prompt engineering |

**Architecture**

- **Harness:** Constrained editing tools; state-management concerns; progressive levels of human oversight
- **Model:** Not specified (see paper)
- **Tool Access:** Constrained editing tools rather than free-form code generation
- **Credentials:** Layered safety controls

**Primitives**

- **Constrained editing tools** — Tools that limit what the agent can change, vs. unconstrained generation
- **Progressive oversight** — Levels of human review that increase trust over time

**Lessons learned**

- Targeted tool design and layered safety controls matter more than prompt tweaks
- Progressive levels of human oversight help build trust before granting autonomy

**Sources**

- [Building an Internal Coding Agent at Zup](https://arxiv.org/abs/2604.09805) _(blog)_

---
